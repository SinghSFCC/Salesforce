<span class="summary-section-label customer-label">
    ${Resource.msg('label.customer.data', 'confirmation', null)}
</span>
<br/>
<div class="summary-details customer">
    <span class="customer-summary-email">${(pdict.customer.profile && pdict.customer.profile.email) ? pdict.customer.profile.email : pdict.order.orderEmail}</span>
</div>
<div class="hidden" id="email_id" data-emailid="${(pdict.customer.profile && pdict.customer.profile.email) ? pdict.customer.profile.email : pdict.order.orderEmail}"></div>
